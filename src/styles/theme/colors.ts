import { theme } from '@chakra-ui/react';

const baseColors = theme.colors;

export const colors = {
  green: {
    ...baseColors.green,
  },
  purple: {
    ...baseColors.purple,
  },
  red: {
    ...baseColors.red,
  },
  gray: {
    ...baseColors.gray,
  },
};
